# Start {{LANGUAGE}} Application

## Instructions

This command starts the {{LANGUAGE}} application in development or production mode.

### Prerequisites

Before running these commands, ensure:

1. Dependencies are installed: `{{INSTALL_DEPS_COMMAND}}`
2. Environment variables are configured in `.env` if needed
3. Configuration file exists (check README.md for location)

### Running the Application

The application can be run in several ways:

## Run

# Run the application directly
{{RUN_COMMAND}}

# Or build first, then run
{{BUILD_COMMAND}}
{{RUN_BUILT_COMMAND}}

# Run a specific subcommand / mode
{{RUN_SUBCOMMAND_EXAMPLE}}
